name: mobile_app_for_shops
description: A modern full-stack Flutter mobile application for shop management system with Firebase integration.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter                    # Internationalization support

  # Firebase (Latest Stable 2025)
  firebase_core: ^3.8.0            # Firebase core services
  firebase_auth: ^5.3.0            # User authentication
  cloud_firestore: ^5.6.0          # Critical multi-user data only
  firebase_storage: ^12.3.0        # Compressed image storage

  # State Management & Architecture
  flutter_bloc: ^9.1.0             # Latest BLoC pattern
  get_it: ^8.0.0                   # Dependency injection
  equatable: ^2.0.7                 # Value equality
  injectable: ^2.5.0                # Code generation for get_it

  # UI & Theming (Latest 2025)
  adaptive_theme: ^4.2.0           # Theme persistence
  flutter_screenutil: ^6.0.0       # Responsive design
  gap: ^3.0.1                      # Spacing utilities

  # Image Handling & Camera (Enhanced for Admin)
  image_picker: ^1.1.2             # Camera and gallery access
  camera: ^0.11.0+1                # Advanced camera features
  photo_view: ^0.15.0              # Image zoom and viewing
  cached_network_image: ^3.4.0     # Image caching
  image: ^4.2.0                    # Image manipulation

  # Image Processing & Compression
  flutter_image_compress: ^2.3.0   # Image compression
  image_editor: ^1.5.0             # Image editing capabilities
  flutter_native_image: ^0.2.0     # Native image processing

  # File Management
  path_provider: ^2.1.4            # Local file storage
  file_picker: ^8.1.2              # Advanced file picking
  permission_handler: ^11.3.1      # Camera and storage permissions

  # Storage & Database
  shared_preferences: ^2.3.3       # Local preferences
  hive: ^2.2.3                     # Local database
  hive_flutter: ^1.1.0
  isar: ^3.1.0+1                   # Alternative fast local DB
  isar_flutter_libs: ^3.1.0+1      # Isar Flutter integration

  # Networking & Connectivity
  connectivity_plus: ^6.1.0        # Network monitoring
  dio: ^5.7.0                      # HTTP client with interceptors
  retrofit: ^4.1.0                  # Type-safe HTTP clients

  # Utilities & Tools
  intl: ^0.19.0                    # Internationalization
  go_router: ^14.6.0                # Navigation
  uuid: ^4.5.0                     # Unique ID generation
  logger: ^2.5.0                   # Logging
  flutter_hooks: ^0.20.5            # Hooks for state management
  freezed_annotation: ^2.4.4       # Code generation

  # UI Components & Animations (Latest 2025)
  flutter_animate: ^4.5.0          # Advanced animations
  lottie: ^3.1.0                   # After Effects animations
  shimmer: ^3.0.0                  # Loading effects
  flutter_staggered_animations: ^1.1.1  # Staggered animations

  # Security & Validation
  crypto: ^3.0.6                   # Cryptographic functions
  local_auth: ^2.3.0               # Biometric authentication
  flutter_secure_storage: ^9.2.2   # Secure storage

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code Generation (Latest 2025)
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  injectable_generator: ^2.6.2
  retrofit_generator: ^8.2.0
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0
  freezed: ^2.5.7
  isar_generator: ^3.1.0+1
  flutter_gen_runner: ^5.8.0

  # Testing & Quality
  mockito: ^5.4.4
  integration_test: ^3.6.0
  very_good_analysis: ^7.0.0       # Linting rules

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/animations/
    - assets/icons/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset path and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Generate translations
flutter_intl:
  enabled: true
  class_name: AppLocalizations
  main_locale: en
  arb_dir: lib/l10n
  output_dir: lib/generated
  output_localization_file: app_localizations.dart